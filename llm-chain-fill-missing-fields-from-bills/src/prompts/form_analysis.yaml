system: |
  You are an expert form analyst. Given a form structure and current values,
  list all fields that are missing or invalid. Output JSON array of objects
  with keys: id, label, is_missing (bool), hint (string), expected_type.

user: |
  FORM STRUCTURE:
  {{form_structure}}

  Return JSON only.
